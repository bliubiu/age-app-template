# 项目上下文

> ⚠️ **模板占位符** — 新项目启动时必须填写真实内容。搜索 `{{` 替换所有占位符。

---

## 项目标识

- **项目名称**：`{{PROJECT_NAME}}`
- **代码仓库**：`{{REPO_URL}}`
- **项目简介**：`{{一句话描述项目是什么}}`

---

## 项目背景

`{{项目发起的背景、要解决的问题、目标用户}}`

---

## 核心目标

- `{{目标1}}`
- `{{目标2}}`
- `{{目标3}}`

---

## 范围边界

- ✅ 包含（In Scope）：
  - `{{包含内容1}}`
- ❌ 不包含（Out of Scope）：
  - `{{不包含内容1}}`

---

## 当前状态

- **Active Requirement**：`{{当前活跃需求文件路径，如 docs/requirements/mvp.md}}`
- **Active Owner Doc**：`{{当前活跃设计文档路径}}`
- **验证命令**：`{{真实可执行的验证命令，如 cargo test / pnpm test}}`

---

## 禁止启动条件

- project-context.md 为空（本文档所有占位符未替换）
- 验证命令仍为占位符
- Active Requirement 为 none
- 需求模糊到需要猜测用户可见行为