# 应用概览

> ⚠️ **模板占位符** — 搜索 `{{` 替换所有占位符。

---

## 应用概述

`{{应用的一句话描述}}`

---

## 用户角色

| 角色 | 描述 | 核心需求 |
|------|------|---------|
| `{{角色1}}` | `{{描述}}` | `{{需求}}` |

---

## 核心流程

### 主流程

```
{{用 ASCII 或 Mermaid 描述核心用户流程}}
```

---

## 页面结构

| 页面 | 路径 | 描述 | 访问角色 |
|------|------|------|---------|
| `{{页面1}}` | `{{/path}}` | `{{描述}}` | `{{角色}}` |

---

## 状态设计

### 核心实体状态机

```
{{实体1}}：状态A → 状态B → 状态C
```