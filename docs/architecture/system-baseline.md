# 系统基线

> ⚠️ **模板占位符** — 新项目启动时根据实际架构填写。搜索 `{{` 替换所有占位符。

---

## 架构概述

`{{简要描述系统整体架构，如：微服务架构 / 模块化单体 / 前后端分离}}`

---

## 模块边界

| 模块       | 职责             | 对外接口         | 依赖           |
| :--------- | :--------------- | :--------------- | :------------- |
| `{{模块1}}` | `{{职责描述}}`  | `{{API/接口}}`  | `{{依赖模块}}` |
| `{{模块2}}` | `{{职责描述}}`  | `{{API/接口}}`  | `{{依赖模块}}` |

---

## 技术栈

详见 `docs/architecture/tech-stack.md`

---

## 数据流

```
{{用 ASCII 或 Mermaid 描述核心数据流}}
```

---

## 部署架构

- **开发环境**：`{{描述}}`
- **测试环境**：`{{描述}}`
- **生产环境**：`{{描述}}`

---

## 关键决策记录

| 决策                         | 理由             | 日期       |
| :--------------------------- | :--------------- | :--------- |
| `{{决策1，如：选用 PostgreSQL}}` | `{{理由}}`      | `{{日期}}` |

---

## 约束

- `{{技术约束1}}`
- `{{技术约束2}}`